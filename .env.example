# Database
ConnectionStrings__Default=Host=localhost;Port=5432;Database=video_streaming;Username=postgres;Password=

# MinIO (internal endpoint for the API process)
MinIO__Endpoint=localhost:9000
MinIO__AccessKey=
MinIO__SecretKey=
MinIO__BucketName=video-streaming

# Video processing (optional overrides)
# VideoProcessing__MaxUploadSizeBytes=4294967296

# Public base URL for HLS/thumbnail links returned to browsers (no trailing slash)
# MinIO__PublicBaseUrl=https://your-domain.example
